services:
  kanbanly:
    image: kanbanly:latest
    ports:
      - "127.0.0.1:3847:3847"
    volumes:
      - kanbanly-data:/root/.kanbanly
    environment:
      - KANBANLY_HOST=127.0.0.1
      - KANBANLY_PORT=3847

volumes:
  kanbanly-data:
